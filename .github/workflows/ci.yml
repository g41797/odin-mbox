name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        opt: [none, minimal, size, speed, aggressive]

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Odin
        uses: laytan/setup-odin@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          release: dev-2026-03

      - name: Build root lib
        shell: bash
        run: |
          if [ "${{ matrix.opt }}" = "none" ]; then
            odin build . -build-mode:lib -vet -strict-style -o:none -debug
          else
            odin build . -build-mode:lib -vet -strict-style -o:${{ matrix.opt }}
          fi

      - name: Build examples/block1
        shell: bash
        run: |
          if [ "${{ matrix.opt }}" = "none" ]; then
            odin build ./examples/block1/ -build-mode:lib -vet -strict-style -o:none -debug
          else
            odin build ./examples/block1/ -build-mode:lib -vet -strict-style -o:${{ matrix.opt }}
          fi

      - name: Build examples/block2
        shell: bash
        run: |
          if [ "${{ matrix.opt }}" = "none" ]; then
            odin build ./examples/block2/ -build-mode:lib -vet -strict-style -o:none -debug
          else
            odin build ./examples/block2/ -build-mode:lib -vet -strict-style -o:${{ matrix.opt }}
          fi

      - name: Build examples/block3
        shell: bash
        run: |
          if [ "${{ matrix.opt }}" = "none" ]; then
            odin build ./examples/block3/ -build-mode:lib -vet -strict-style -o:none -debug
          else
            odin build ./examples/block3/ -build-mode:lib -vet -strict-style -o:${{ matrix.opt }}
          fi

      - name: Test tests/block1
        shell: bash
        run: |
          if [ "${{ matrix.opt }}" = "none" ]; then
            odin test ./tests/block1/ -vet -strict-style -disallow-do -o:none -debug
          else
            odin test ./tests/block1/ -vet -strict-style -disallow-do -o:${{ matrix.opt }}
          fi

      - name: Test tests/block2
        shell: bash
        run: |
          if [ "${{ matrix.opt }}" = "none" ]; then
            odin test ./tests/block2/ -vet -strict-style -disallow-do -o:none -debug
          else
            odin test ./tests/block2/ -vet -strict-style -disallow-do -o:${{ matrix.opt }}
          fi

      - name: Test tests/block3
        shell: bash
        run: |
          if [ "${{ matrix.opt }}" = "none" ]; then
            odin test ./tests/block3/ -vet -strict-style -disallow-do -o:none -debug
          else
            odin test ./tests/block3/ -vet -strict-style -disallow-do -o:${{ matrix.opt }}
          fi
