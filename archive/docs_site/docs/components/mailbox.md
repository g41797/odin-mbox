# Mailbox

## Purpose

Document the Mailbox component: bounded, typed message queue with ownership semantics.

## To be written

- API surface (create, send, recv, close)
- Capacity and flow control behavior
- Lifecycle (open → active → closed)
- Usage examples
