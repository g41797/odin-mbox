{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"matryoshka","text":""},{"location":"#purpose","title":"Purpose","text":"<p>Entry point for the matryoshka concurrency toolkit documentation.</p>"},{"location":"#to-be-written","title":"To be written","text":"<ul> <li>Project overview and motivation</li> <li>Key design principles</li> <li>Quick navigation guide</li> <li>Links to getting started</li> </ul>"},{"location":"api/","title":"API Reference","text":"<p>Heads up</p> <p>The API reference is a standalone generated site (odin-doc), separate from this documentation.</p> <p>Your options:</p> <ul> <li>Open here \u2014 navigates away; use the browser Back button to return</li> <li>Open in new tab \u2014 keeps this docs site open alongside the API reference</li> </ul> <p>Open here Open in new tab</p>"},{"location":"components/loop/","title":"Loop","text":""},{"location":"components/loop/#purpose","title":"Purpose","text":"<p>Document the Loop component for running recurring or continuous processing.</p>"},{"location":"components/loop/#to-be-written","title":"To be written","text":"<ul> <li>Loop lifecycle (start, tick, stop)</li> <li>Integration with mailbox and wakeuper</li> <li>Error handling and restart policy</li> <li>Usage examples</li> </ul>"},{"location":"components/mailbox/","title":"Mailbox","text":""},{"location":"components/mailbox/#purpose","title":"Purpose","text":"<p>Document the Mailbox component: bounded, typed message queue with ownership semantics.</p>"},{"location":"components/mailbox/#to-be-written","title":"To be written","text":"<ul> <li>API surface (create, send, recv, close)</li> <li>Capacity and flow control behavior</li> <li>Lifecycle (open \u2192 active \u2192 closed)</li> <li>Usage examples</li> </ul>"},{"location":"components/mpsc/","title":"MPSC","text":""},{"location":"components/mpsc/#purpose","title":"Purpose","text":"<p>Document the MPSC (multi-producer, single-consumer) queue component.</p>"},{"location":"components/mpsc/#to-be-written","title":"To be written","text":"<ul> <li>Difference from Mailbox</li> <li>When to use MPSC vs Mailbox</li> <li>API reference</li> <li>Concurrency guarantees</li> </ul>"},{"location":"components/pool/","title":"Pool","text":""},{"location":"components/pool/#purpose","title":"Purpose","text":"<p>Document the Pool component for managing reusable items.</p>"},{"location":"components/pool/#to-be-written","title":"To be written","text":"<ul> <li>What Pool manages (items, not messages)</li> <li>Acquire and release API</li> <li>Lifecycle hooks (T_HOOKS)</li> <li>Integration with worker pool idiom</li> </ul>"},{"location":"components/wakeuper/","title":"Wakeuper","text":""},{"location":"components/wakeuper/#purpose","title":"Purpose","text":"<p>Document the Wakeuper component for timer-based or external wakeup signals.</p>"},{"location":"components/wakeuper/#to-be-written","title":"To be written","text":"<ul> <li>Purpose: unblock a waiting consumer on schedule or on demand</li> <li>API (arm, disarm, wait)</li> <li>Integration with loop and event loop idioms</li> <li>Edge cases (missed wakeups, shutdown)</li> </ul>"},{"location":"concepts/mental_model/","title":"Mental Model","text":""},{"location":"concepts/mental_model/#purpose","title":"Purpose","text":"<p>Establish the core mental model for reasoning about concurrency in matryoshka.</p>"},{"location":"concepts/mental_model/#to-be-written","title":"To be written","text":"<ul> <li>What the toolkit is built around (messages, ownership, structured lifecycles)</li> <li>How to think about components as isolated actors</li> <li>Contrast with shared-memory threading models</li> <li>Diagrams of message flow</li> </ul>"},{"location":"concepts/message_passing/","title":"Message Passing","text":""},{"location":"concepts/message_passing/#purpose","title":"Purpose","text":"<p>Describe how message passing is used as the primary coordination mechanism.</p>"},{"location":"concepts/message_passing/#to-be-written","title":"To be written","text":"<ul> <li>Send and receive primitives</li> <li>Blocking vs non-blocking variants</li> <li>Backpressure and capacity</li> <li>Error and timeout handling</li> </ul>"},{"location":"concepts/ownership/","title":"Ownership","text":""},{"location":"concepts/ownership/#purpose","title":"Purpose","text":"<p>Explain how ownership of messages and resources is managed in matryoshka.</p>"},{"location":"concepts/ownership/#to-be-written","title":"To be written","text":"<ul> <li>Single-owner semantics for messages</li> <li>Transfer of ownership across mailboxes</li> <li>Lifecycle implications (who frees, when)</li> <li>Common mistakes and how the toolkit prevents them</li> </ul>"},{"location":"guides/building_pipeline/","title":"Building a Pipeline","text":""},{"location":"guides/building_pipeline/#purpose","title":"Purpose","text":"<p>Guide the user through constructing a multi-stage processing pipeline with matryoshka.</p>"},{"location":"guides/building_pipeline/#to-be-written","title":"To be written","text":"<ul> <li>Designing stages and their mailboxes</li> <li>Wiring stages together</li> <li>Handling errors mid-pipeline</li> <li>Shutdown and drain</li> </ul>"},{"location":"guides/event_loop/","title":"Event Loop Guide","text":""},{"location":"guides/event_loop/#purpose","title":"Purpose","text":"<p>Guide the user through building a single-threaded event loop using matryoshka primitives.</p>"},{"location":"guides/event_loop/#to-be-written","title":"To be written","text":"<ul> <li>Setting up a loop with multiple mailboxes</li> <li>Prioritizing event sources</li> <li>Integrating the Wakeuper for timers</li> <li>Safe shutdown sequence</li> </ul>"},{"location":"guides/getting_started/","title":"Getting Started","text":""},{"location":"guides/getting_started/#purpose","title":"Purpose","text":"<p>Walk a new user through installing and running their first matryoshka program.</p>"},{"location":"guides/getting_started/#to-be-written","title":"To be written","text":"<ul> <li>Prerequisites (Odin toolchain version)</li> <li>Adding matryoshka to a project</li> <li>Hello-world example with a mailbox</li> <li>Where to go next</li> </ul>"},{"location":"idioms/","title":"Idioms Overview","text":"<p>This section provides a quick reference for matryoshka idioms. Each idiom has a short tag for <code>grep</code>.</p> <p>These are not laws. No one is forced to follow them. They are patterns that have worked. Take what helps, ignore the rest.</p>"},{"location":"idioms/#the-golden-rule-one-variable-lifecycle","title":"The Golden Rule: One Variable Lifecycle","text":"<p>Mantra: One convention across all transfer points. Same variable, whole lifetime, misuse detected at every boundary.</p>"},{"location":"idioms/#the-rule","title":"The Rule","text":"<ol> <li><code>^Maybe(^T)</code> replaces <code>^T</code> return: Wherever an item is acquired or transferred (<code>get</code>, <code>receive</code>), it is passed as a parameter, not returned.</li> <li>Check on Entry: Every proc checks <code>itm^ != nil</code> on entry. If the caller still holds an item, it returns <code>.Already_In_Use</code>. This prevents overwriting valid data.</li> <li>One Variable: The caller uses a single variable from <code>get</code> -&gt; <code>send</code> -&gt; <code>wait_receive</code> -&gt; <code>put</code> -&gt; <code>dispose</code>.</li> </ol>"},{"location":"idioms/#lifecycle-in-one-variable","title":"Lifecycle in one variable","text":"<pre><code>m: Maybe(^Itm)\n\n// 1. Acquire\nget(&amp;p, &amp;m)            // Returns .Already_In_Use if m != nil\ndefer dispose(&amp;m)      // Safety net: no-op if transferred, cleans up if stuck\n\n// 2. Use\n// fill m^ ...\n\n// 3. Transfer\nsend(&amp;mb, &amp;m)          // m = nil on success (dispose becomes no-op)\n                       // m != nil on failure (dispose cleans up)\n\n// 4. Loop\n// On next iteration, get(&amp;p, &amp;m) verifies m is nil.\n</code></pre>"},{"location":"idioms/#building-blocks","title":"Building Blocks","text":"<p>matryoshka has five object types. Every concurrent system built with this library uses them. Understanding what each one is\u2014and why it exists\u2014makes the idioms easier to follow.</p> <ul> <li>Master: The actor. It has the logic. You define this as a pattern.</li> <li>Thread: A container. It runs one Master proc and nothing else.</li> <li>Item (<code>Itm</code>): Any reusable object managed by a pool.</li> <li>Pool: Holds a set of reusable items.</li> <li>Mbox (Mailbox): Moves a pointer from one Master to another.</li> </ul>"},{"location":"idioms/#marker-scheme","title":"Marker Scheme","text":"<p>Each idiom has a short tag. The tag appears as a comment at the relevant line in code:</p> <pre><code>// [itc: &lt;tag&gt;]\n</code></pre> <p>To find all usages of one idiom: <code>grep -r \"\\[itc: maybe-container\\]\" examples/ tests/</code></p>"},{"location":"idioms/#quick-reference","title":"Quick Reference","text":"Tag Name One line <code>maybe-container</code> Maybe as container Wrap a heap pointer in <code>Maybe(^T)</code> before any pointer-transferring call. <code>defer-put</code> defer with pool.put Use <code>defer pool.put</code> to return to pool in all paths. <code>dispose-contract</code> dispose signature contract A dispose proc takes <code>^Maybe(^T)</code>. Nil inner is a no-op. Sets inner to nil on return. <code>defer-dispose</code> defer with dispose Use <code>defer dispose(&amp;m)</code> so cleanup runs in all paths. <code>disposable-itm</code> DisposableItm full lifecycle Items with internal heap resources use a full, careful lifecycle. <code>foreign-dispose</code> foreign item with resources When put returns a foreign pointer, call dispose, not free. <code>reset-vs-dispose</code> reset vs dispose <code>reset</code> clears state for reuse. <code>dispose</code> frees internal resources permanently. <code>dispose-optional</code> dispose is advice <code>dispose</code> is called by the caller, never by pool or mailbox. <code>heap-master</code> ITC participants in a heap-allocated struct Heap-allocate the struct that owns ITC participants. <code>thread-container</code> thread is just a container for its master A thread proc only casts rawptr to <code>^Owner</code>. <code>errdefer-dispose</code> conditional defer for factory procs Use named return + <code>defer if !ok { dispose(...) }</code> in factory procs. <code>defer-destroy</code> destroy resources at scope exit Register <code>defer destroy</code> for resources to guarantee shutdown. <code>t-hooks</code> T_Hooks pattern Define factory/reset/dispose as a <code>::</code> constant next to the type. <p>This overview provides a high-level look. See the specific pages for Ownership and Lifecycle and Concurrency Structure for detailed explanations of each idiom.</p>"},{"location":"idioms/ownership/","title":"Ownership and Lifecycle Idioms","text":"<p>These idioms focus on the safe transfer of ownership and the management of an object's lifecycle, especially when dealing with pooled, disposable items.</p>"},{"location":"idioms/ownership/#ownership-model","title":"Ownership Model","text":"Tag Name One line <code>maybe-container</code> Maybe as container Wrap a heap pointer in <code>Maybe(^T)</code> before any pointer-transferring call. <code>dispose-contract</code> dispose signature contract A dispose proc takes <code>^Maybe(^T)</code>. Nil inner is a no-op. Sets inner to nil on return. <code>defer-dispose</code> defer with dispose Use <code>defer dispose(&amp;m)</code> so cleanup runs in all paths. <code>errdefer-dispose</code> conditional defer for factory procs Use named return + <code>defer if !ok { dispose(...) }</code> when a factory proc creates and returns a master. <code>dispose-optional</code> dispose is advice dispose is called by the caller, never by pool or mailbox."},{"location":"idioms/ownership/#maybe-container-maybe-as-container","title":"<code>maybe-container</code> \u2014 Maybe as container","text":"<p>Problem: You have a <code>^T</code> from <code>new</code> or <code>pool_get</code>. You want to pass it to <code>send</code> or <code>push</code> safely. Fix: Wrap it in <code>Maybe(^T)</code> before any pointer-transferring call.</p> <pre><code>// [itc: maybe-container]\nm: Maybe(^Itm) = new(Itm)\nmbox_send(&amp;mb, &amp;m)\n// m is nil here \u2014 transfer complete, mailbox holds the pointer\n</code></pre>"},{"location":"idioms/ownership/#dispose-contract-dispose-signature-contract","title":"<code>dispose-contract</code> \u2014 dispose signature contract","text":"<p>Problem: A struct contains internal heap resources. You need a proc to free them all safely. Fix: Write a dispose proc that follows the <code>^Maybe(^T)</code> contract. It takes <code>^Maybe(^T)</code>, is a no-op if the inner pointer is <code>nil</code>, and sets the inner pointer to <code>nil</code> on return.</p>"},{"location":"idioms/ownership/#defer-dispose-defer-with-dispose","title":"<code>defer-dispose</code> \u2014 defer with dispose","text":"<p>Problem: You fill an item with internal heap resources before sending. If <code>send</code> fails, you need to clean up. Fix: Register <code>dispose</code> via <code>defer</code> right after wrapping in <code>Maybe</code>.</p> <pre><code>m: Maybe(^DisposableItm) = itm\ndefer disposable_dispose(&amp;m)  // [itc: defer-dispose]\n// ...\nif mbox_send(&amp;mb, &amp;m) { /* success */ }\n// If send fails, `m` is not nil, and the deferred dispose runs.\n// If send succeeds, `m` is nil, and the deferred dispose is a no-op.\n</code></pre>"},{"location":"idioms/ownership/#object-lifecycle-pool-model","title":"Object Lifecycle / Pool Model","text":"Tag Name One line <code>defer-put</code> defer with pool.put Use <code>defer pool.put</code> to return to pool in all paths. <code>disposable-itm</code> DisposableItm full lifecycle Items with internal heap resources use a full, careful lifecycle. <code>foreign-dispose</code> foreign item with resources When put returns a foreign pointer, call dispose, not free. <code>reset-vs-dispose</code> reset vs dispose <code>reset</code> clears state for reuse. <code>dispose</code> frees internal resources permanently. <code>t-hooks</code> T_Hooks pattern Define factory/reset/dispose as a <code>::</code> constant next to the type."},{"location":"idioms/ownership/#defer-put-defer-with-poolput","title":"<code>defer-put</code> \u2014 defer with pool.put","text":"<p>Problem: You <code>get</code> an item from the pool and must return it in all paths. Fix: Use <code>defer pool.put</code> immediately after acquisition. If the item is successfully transferred elsewhere (e.g., via <code>mbox_send</code>), its <code>Maybe</code> container becomes <code>nil</code> and the <code>put</code> becomes a no-op.</p>"},{"location":"idioms/ownership/#reset-vs-dispose-reset-vs-dispose","title":"<code>reset-vs-dispose</code> \u2014 reset vs dispose","text":"<p>Problem: It is easy to confuse <code>reset</code> (for reuse) with <code>dispose</code> (for permanent cleanup). Fix: Keep them separate. Never free internal resources in <code>reset</code>. - <code>reset</code>: Prepares item for reuse. Called by the pool before returning an item to the free list. - <code>dispose</code>: Frees internal resources permanently. Called by the pool on permanent destruction.</p>"},{"location":"idioms/ownership/#t-hooks-t_hooks-pattern","title":"<code>t-hooks</code> \u2014 T_Hooks pattern","text":"<p>Problem: A complex item type needs custom logic for allocation, reset, and disposal. Fix: Define <code>factory</code>, <code>reset</code>, and <code>dispose</code> procedures and register them in a <code>pool.T_Hooks</code> constant. Pass this constant to <code>pool_init</code>. The pool will then call your custom procedures at the appropriate lifecycle points.</p>"},{"location":"idioms/structure/","title":"Concurrency Structure Idioms","text":"<p>These idioms relate to the high-level organization of concurrent programs, focusing on how threads and shared resources are structured for safety and clarity.</p> Tag Name One line <code>heap-master</code> ITC participants in a heap-allocated struct Heap-allocate the struct that owns ITC participants when its address is shared with spawned threads. <code>thread-container</code> thread is just a container for its master A thread proc only casts rawptr to <code>^Owner</code>. No ITC participants declared as stack locals. <code>defer-destroy</code> destroy resources at scope exit Register <code>defer destroy</code> for pools/mboxes/loops to guarantee shutdown in all paths."},{"location":"idioms/structure/#heap-master-itc-participants-in-a-heap-allocated-struct","title":"<code>heap-master</code> \u2014 ITC participants in a heap-allocated struct","text":"<p>Problem: Threads must not reference the stack memory of a procedure that might exit before the threads complete. Fix: <code>new(Master)</code> \u2014 heap-allocate the owner struct that contains the <code>Pool</code>, <code>Mbox</code>, and other shared resources. Pass a pointer to this struct to your threads. Call a <code>master_dispose</code> function after joining all threads to clean up.</p>"},{"location":"idioms/structure/#thread-container-thread-is-just-a-container-for-its-master","title":"<code>thread-container</code> \u2014 thread is just a container for its master","text":"<p>Problem: Pointers to thread-local stack participants can accidentally escape the thread's frame, leading to use-after-free bugs. Fix: Move all ITC participants (Pools, Mboxes, etc.) into the heap-allocated <code>Master</code> struct. The thread procedure should do nothing but cast its <code>rawptr</code> argument to a <code>^Master</code> and call a <code>master_run</code> function. This keeps all shared state centrally managed.</p> <pre><code>// The thread procedure is just a thin wrapper\nproc(data: rawptr) {\n    c := (^Master)(data) // [itc: thread-container]\n    master_run(c)        // All logic is in the master\n}\n</code></pre>"},{"location":"idioms/structure/#defer-destroy-destroy-resources-at-scope-exit","title":"<code>defer-destroy</code> \u2014 destroy resources at scope exit","text":"<p>Problem: Resources like pools and mailboxes must be shut down in all code paths to prevent leaks or deadlocks. Fix: Register <code>destroy</code> with <code>defer</code> immediately after successful initialization. This guarantees the cleanup logic runs even if the function exits early.</p> <pre><code>mbox_init(&amp;mb)\ndefer mbox_destroy(&amp;mb) // [itc: defer-destroy]\n\npool_init(&amp;p)\ndefer pool_destroy(&amp;p)\n</code></pre>"},{"location":"patterns/event_loop/","title":"Event Loop","text":""},{"location":"patterns/event_loop/#purpose","title":"Purpose","text":"<p>Describe the event loop idiom for handling multiple event sources in a single goroutine.</p>"},{"location":"patterns/event_loop/#to-be-written","title":"To be written","text":"<ul> <li>Single-threaded event dispatch model</li> <li>How to integrate mailbox polling</li> <li>Timeout and timer events</li> <li>Graceful shutdown pattern</li> </ul>"},{"location":"patterns/pipelines/","title":"Pipelines","text":""},{"location":"patterns/pipelines/#purpose","title":"Purpose","text":"<p>Describe the pipeline idiom for chaining processing stages.</p>"},{"location":"patterns/pipelines/#to-be-written","title":"To be written","text":"<ul> <li>What a pipeline is in matryoshka terms</li> <li>How to compose stages using mailboxes</li> <li>Backpressure propagation</li> <li>Example pipeline skeleton</li> </ul>"},{"location":"patterns/worker_pool/","title":"Worker Pool","text":""},{"location":"patterns/worker_pool/#purpose","title":"Purpose","text":"<p>Describe the worker pool idiom for bounded parallel processing.</p>"},{"location":"patterns/worker_pool/#to-be-written","title":"To be written","text":"<ul> <li>Pool structure and sizing</li> <li>Task distribution strategy</li> <li>Result collection</li> <li>Shutdown and drain sequence</li> </ul>"}]}